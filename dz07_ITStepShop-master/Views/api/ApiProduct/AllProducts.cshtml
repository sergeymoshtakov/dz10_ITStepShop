@model IEnumerable<Models.Data.Product>

<h1>All Products</h1>

@foreach (var product in Model)
{
    <div>
        <h4>@product.Name</h4>
        <p>Category: @product.Category.CategoryName</p>
    </div>
}
